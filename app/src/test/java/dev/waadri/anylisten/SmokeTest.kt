package dev.waadri.anylisten

import org.junit.Assert.assertEquals
import org.junit.Test

/**
 * Placeholder test so the CI unit-test step has something to run in Phase 0.
 * Real coverage lands with the protocol layer (Phase 1).
 */
class SmokeTest {
    @Test
    fun gradleSetupWorks() {
        assertEquals(4, 2 + 2)
    }
}
